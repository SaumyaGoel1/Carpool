# API base URL (used for sign-in, sign-up, and all API calls).
# If you run the app at http://localhost:5173, the code defaults to http://localhost:3000.
# Set this when your API is on a different host or port.
VITE_API_URL=http://localhost:3000
